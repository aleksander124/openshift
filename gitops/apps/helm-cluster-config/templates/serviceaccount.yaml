# {{- range .Values.users }}

{{ range $path, $_ :=  $.Files.Glob  "cluster-values/**.yaml" }}
{{ $user := ($.Files.Get $path ) | fromYaml }}

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .name }}
  namespace: default
{{- end }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: global-deployment
  namespace: default