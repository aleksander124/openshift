apiVersion: v1
kind: ResourceQuota
metadata:
  name: new-test-namespace-quota.yaml
  namespace: new-test-namespace
spec:
  hard:
    requests.cpu: "1"
    requests.memory: "1Gi"
    requests.ephemeral-storage: "5Gi"
    limits.cpu: "3"
    limits.memory: "3Gi"
    pods: "10"
    services: "10"
    configmaps: "10"