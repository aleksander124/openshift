# commands for openshift to create secrete
oc create secret docker-registry \
    --docker-server=<repository name like: docker.io> \
    --docker-username=<user name> \
    --docker-password=***** \
    --docker-email=unused \
    acr-secret